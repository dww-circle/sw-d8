{% extends 'page.html.twig' %}

{% block main %}
  <div class="story-label">{{ 'Recent Stories on'|t }}</div>
  <h1 class="page-title page-title-full">{{ page_title }}</h1>
  {{ page.content }}
  {{ page.sidebar }}
{% endblock main %}
